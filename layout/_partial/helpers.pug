mixin a_with_current(href, content, id)
  - var url = url_for(href)
  a(href=url)
    i(class='fa' + ' ' + id)= ' ' + content

mixin title(page)
  if page
    if page.title
      = page.title
    else
      = _p('no-title')
